// Single source of truth for the canonical site URL, used by metadata, the
// sitemap, robots.txt and OG image generation. Swapping to a custom domain
// later is a one-line change here rather than a multi-file find/replace.
export const SITE_URL = "https://annaizu-sponsor-checker-seven.vercel.app";
